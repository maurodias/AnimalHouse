<h1>Listing tratamentos</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Detalhes</th>
    <th>Item</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tratamentos.each do |tratamento| %>
  <tr>
    <td><%= tratamento.nome %></td>
    <td><%= tratamento.detalhes %></td>
    <td><%= tratamento.item %></td>
    <td><%= link_to 'Show', tratamento %></td>
    <td><%= link_to 'Edit', edit_tratamento_path(tratamento) %></td>
    <td><%= link_to 'Destroy', tratamento, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tratamento', new_tratamento_path %>
