= semantic_form_for(@raca) do |f| 
  = f.input :nome, :label => 'Nome'
  = f.input :origem, :label => 'Origem'
  = f.input :detalhes, :label => 'Detalhes'
  = f.input :item, :member_label => :id, :label => 'Item'
  = f.input :animal, :member_label => :nomePopular, :label => 'Animal'
  = f.submit 'Salvar'
